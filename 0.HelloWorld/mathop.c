#include <stdio.h>

int main()
{
    int x, y;
   
    x = 10;
    y = 5;
    
    int sum = x + y;
    int difference = x - y;

    printf("the sum of numbers is %d\n", sum);
    printf("the difference of numbers is %d\n", difference);
    return (0);
}